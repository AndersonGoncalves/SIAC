@model SiacWeb.Models.ViewModels.UsuarioFormViewModel

@{
    ViewData["Title"] = "Usuário";
}

<h2>@ViewData["Title"]</h2>

<form asp-action="Edit" class="form-horizontal jumbotron">
    <fieldset>
        <input type="hidden" asp-for="IdentityUser.Id" />

        <div class="form-group">
            <label asp-for="IdentityUser.Id" class="col-lg-2 control-label" for="inputSmall"></label>
            <div class="col-lg-10">
                <input asp-for="IdentityUser.Id" class="form-control input-sm" id="disabledInput" disabled="" />
                <span asp-validation-for="IdentityUser.Id" class="text-danger"></span>
            </div>
        </div>

        <div class="form-group">
            <label asp-for="IdentityUser.UserName" class="col-lg-2 control-label" for="inputSmall"></label>
            <div class="col-lg-10">
                <input asp-for="IdentityUser.UserName" class="form-control input-sm" id="inputSmall" placeholder="Nome" />
                <span asp-validation-for="IdentityUser.UserName" class="text-danger"></span>
            </div>
        </div>

        <div class="form-group">
            <div class="col-lg-10 col-lg-offset-2">
                <a asp-action="Index" class="btn btn-default btn-sm">Cancelar</a>
                <input type="submit" value="Salvar" class="btn btn-primary btn-sm">
            </div>
        </div>
    </fieldset>
</form>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}